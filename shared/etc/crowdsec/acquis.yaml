# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  CrowdSec — Log Acquisition                                               ║
# ╠══════════════════════════════════════════════════════════════════════════════╣
# ║  Pfad:         /etc/crowdsec/acquis.yaml                                   ║
# ║  Repo:         shared/etc/crowdsec/acquis.yaml                             ║
# ║  Autor:        MR-ByteZ                                                    ║
# ║  Version:      0.4.0                                                       ║
# ║  Erstellt:     2026-03-04                                                  ║
# ║  Aktualisiert: 2026-03-06                                                  ║
# ║  Hinweis:      acquis.d/ wird bei Installation auto-generiert!             ║
# ║                Dort liegen: setup.sshd.yaml, setup.linux.yaml,            ║
# ║                setup.auditd.yaml — NICHT doppelt pflegen!                 ║
# ║  Zweck:        Log-Quellen fuer CrowdSec Security Engine (nativ)           ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# --- Traefik Access-Log (JSON-Format) ---
filenames:
  - /var/log/traefik/access.log
labels:
  type: traefik

# --- SSH / sshd ---
# NICHT hier definieren! Wird automatisch durch acquis.d/setup.sshd.yaml
# abgedeckt (auto-generiert bei CrowdSec-Installation via sshd-Collection).
# Doppelte Acquisition fuehrt zu doppelter Log-Verarbeitung!
