# ┌─────────────────────────────────────────────────────────┐
# │  MR-ByteZ — Reflector Konfiguration                   │
# └─────────────────────────────────────────────────────────┘
# Datei:        reflector.conf
# Pfad:         shared/etc/xdg/reflector/reflector.conf
# Autor:        MR-ByteZ
# Version:      0.1.0
# Erstellt:     2026-03-04
# Zweck:        Reflector-Einstellungen fuer reflector.timer
#               Deployed via deploy.fish nach /etc/xdg/reflector/reflector.conf

--age 12
--protocol https
--sort rate
--number 20
--save /etc/pacman.d/mirrorlist
