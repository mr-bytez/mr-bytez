# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  Traefik — Statische Konfiguration                                         ║
# ╠══════════════════════════════════════════════════════════════════════════════╣
# ║  Pfad:         projects/infrastructure/n8-vps/stacks/traefik/config/       ║
# ║  Autor:        MR-ByteZ                                                    ║
# ║  Version:      0.2.0                                                       ║
# ║  Erstellt:     2026-03-01                                                  ║
# ║  Aktualisiert: 2026-03-05                                                  ║
# ║  Zweck:        Traefik v3 statische Config (EntryPoints, Provider, ACME)   ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# --- API & Dashboard ---
api:
  dashboard: true
  insecure: false

# --- EntryPoints ---
entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
    http:
      tls:
        certResolver: letsencrypt-production
    transport:
      respondingTimeouts:
        readTimeout: 30s
        writeTimeout: 60s
        idleTimeout: 120s
      lifeCycle:
        graceTimeOut: 30s
      keepAliveMaxRequests: 1000
      keepAliveMaxTime: 600s

# --- Providers ---
providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: mrbz-proxy-net
  file:
    directory: "/etc/traefik/dynamic/"
    watch: true

# --- Certificates Resolvers (ACME) ---
certificatesResolvers:
  letsencrypt-production:
    acme:
      email: "traefik@mr-bytez.de"
      storage: "/letsencrypt/acme.json"
      dnsChallenge:
        provider: hetzner
        # DNS-Aufloesung ueber Docker-internen DNS (→ Host Unbound)
        # Kein externer Resolver noetig, UFW blockt Outbound DNS

# --- Logging ---
log:
  level: "INFO"
  filePath: "/var/log/traefik/error.log"

accessLog:
  filePath: "/var/log/traefik/access.log"
  format: json
  bufferingSize: 100

# --- CrowdSec Bouncer Plugin ---
experimental:
  plugins:
    crowdsec-bouncer:
      moduleName: github.com/maxlerebourg/crowdsec-bouncer-traefik-plugin
      version: v1.5.1
